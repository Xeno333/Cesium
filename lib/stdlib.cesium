c{
    #include<stdio.h>
}c

cref func fopen(i8* fn, i8* op) : i64
cref func fclose(u64 f) : i32


func fget(u8* fn, mut i8* block, u64 size) : u8* {
    u64 f = (fopen(fn, "rb") : u64)


    fclose(f)

    return block
}

func fsize(u8* fb) : i64